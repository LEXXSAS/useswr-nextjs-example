Nextjs get data and search with redux and useSwr example with pokeapi

> [!NOTE]
> To start the project you need to run:

#### On client folder
command "node init -y" and "node install"
after "npm run dev"
